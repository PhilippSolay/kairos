# Copy to .env and fill in. BOTH are required — without them Kiros runs with NO auth,
# which must never happen on a public deploy.
#
#   cp .env.example .env
#   # generate a strong password:  openssl rand -base64 24
#
KIROS_AUTH_USER=philipp
KIROS_AUTH_PASS=replace-with-a-long-random-string
